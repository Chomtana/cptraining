{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "C++ Build",
            "type": "shell",
            "command": "G:\\cptraining\\VSCodeCompile\\runner.bat",
            "args": [
                "${file}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "options": {
                "cwd": "${fileDirname}"
            }
        },
        {
            "label": "C++ Run",
            "type": "shell",
            "command": "G:\\cptraining\\VSCodeCompile\\runnerfile.bat",
            "args": [
                "${file}", "G:\\cptraining\\input.in"
            ],
            "group": {
                "kind": "test",
                "isDefault": true
            },
            "options": {
                "cwd": "${fileDirname}"
            }
        },
        {
            "label": "C++ Run Out to file",
            "type": "shell",
            "command": "G:\\cptraining\\VSCodeCompile\\runnerout.bat",
            "args": [
                "${file}", "G:\\cptraining\\input.in"
            ],
            "options": {
                "cwd": "${fileDirname}"
            }
        },
        {
            "label": "C++ Run In file Out to file",
            "type": "shell",
            "command": "G:\\cptraining\\VSCodeCompile\\runnerfileout.bat",
            "args": [
                "${file}", "G:\\cptraining\\input.in"
            ],
            "options": {
                "cwd": "${fileDirname}"
            }
        }
    ]
}
